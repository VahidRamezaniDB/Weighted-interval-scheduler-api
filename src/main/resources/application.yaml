server:
  port: 8081
  servlet:
    context-path: /api

spring:
  rabbitmq:
    addresses: localhost:5672
    username: guest
    password: guest
    virtual-host: /

app:
  read-only: false
  rabbit:
    addresses: localhost:5672
    username: guest
    password: guest
    virtual-host: /
